assemble_baseline {
    tasks = ["assemble"]
    cleanup-tasks = ["clean"]
    git-checkout = {
        build = "baseline"
    }
}

assemble_error_prone {
    tasks = ["assemble"]
    cleanup-tasks = ["clean"]
    git-checkout = {
        build = "error-prone-default"
    }   
}

assemble_error_prone_disable_expensive_checks {
    tasks = ["assemble"]
    cleanup-tasks = ["clean"]
    git-checkout = {
        build = "error-prone-disable-expensive-checks"
    }   
}
